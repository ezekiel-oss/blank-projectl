{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm build",
  "installCommand": "bash -lc \"corepack enable && corepack prepare pnpm@9.12.0 --activate && echo Using Node $(node -v) and pnpm $(pnpm -v) && pnpm install --no-frozen-lockfile\"",
  "env": {
    "NEXT_PUBLIC_APP_NAME": "Conversational AI"
  },
  "build": {
    "env": {
      "NODE_VERSION": "20.11.1"
    }
  }
}